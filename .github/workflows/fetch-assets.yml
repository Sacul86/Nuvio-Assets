name: Fetch Nuvio Assets

# Manual trigger only — click "Run workflow" in the Actions tab.
# API keys come from repo Settings → Secrets → Actions, so the only thing
# you ever need to decide at run time is whether to re-fetch existing tiles.
on:
  workflow_dispatch:
    inputs:
      overwrite:
        description: "Re-fetch images that already exist?"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests Pillow

      - name: Download Bebas Neue font
        run: |
          curl -fsSL -o /tmp/BebasNeue-Regular.ttf \
            https://github.com/google/fonts/raw/main/ofl/bebasneue/BebasNeue-Regular.ttf
          ls -la /tmp/BebasNeue-Regular.ttf

      - name: Verify secrets are available
        run: |
          if [ -z "${{ secrets.TMDB_API_KEY }}" ]; then
            echo "::error::TMDB_API_KEY secret is missing or empty. Add it at Settings -> Secrets and variables -> Actions."
            exit 1
          fi
          if [ -z "${{ secrets.FANART_API_KEY }}" ]; then
            echo "::error::FANART_API_KEY secret is missing or empty."
            exit 1
          fi
          if [ -z "${{ secrets.MDBLIST_API_KEY }}" ]; then
            echo "::warning::MDBLIST_API_KEY not set; script will fall back to TMDB Discover only."
          fi
          echo "TMDB + fanart present."

      - name: Generate assets
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          FANART_API_KEY: ${{ secrets.FANART_API_KEY }}
          MDBLIST_API_KEY: ${{ secrets.MDBLIST_API_KEY }}
          IDEOGRAM_API_KEY: ${{ secrets.IDEOGRAM_API_KEY }}
          OVERWRITE: ${{ inputs.overwrite && '1' || '0' }}
        run: python fetch_all_assets.py 2>&1 | tee /tmp/fetch.log

      - name: Commit and push images
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/
          if git diff --staged --quiet; then
            echo "No new or changed images to commit."
          else
            git commit -m "Update Nuvio collection assets [skip ci]"
            git push
          fi
